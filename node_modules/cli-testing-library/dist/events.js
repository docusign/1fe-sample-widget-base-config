"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.fireEvent = void 0;
var _eventMap = require("./event-map");
/**
 * Silence TypeScript errors
 * @type {*}
 */
const fireEvent = (instance, event, props = undefined) => {
  _eventMap.eventMap[event](instance, props);
};
exports.fireEvent = fireEvent;
Object.entries(_eventMap.eventMap).forEach(([eventName, eventFn]) => {
  fireEvent[eventName] = (instance, ...props) => {
    eventFn(instance, ...props);
  };
});