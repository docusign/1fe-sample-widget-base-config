"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateCombinations = void 0;
function recursiveGenerateCombinations(arrays, index = 0, current = []) {
    if (index === arrays.length) {
        return [current];
    }
    const currentArray = arrays[index].length === 0 ? [""] : arrays[index];
    const results = [];
    for (const element of currentArray) {
        const combinations = recursiveGenerateCombinations(arrays, index + 1, [
            ...current,
            element,
        ]);
        results.push(...combinations);
    }
    return results;
}
function generateCombinations(arrays) {
    return recursiveGenerateCombinations(arrays)
        .map((combination) => combination.join(""))
        .filter((combination) => combination !== "");
}
exports.generateCombinations = generateCombinations;
//# sourceMappingURL=generateCombinations.js.map